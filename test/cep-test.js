var cep = require('../index');var assert = require('assert');var expect = require('chai').expect;describe('myCep', function() {    it('Buscar cep via JSON', function() {        cep.byJson('28035053')            .catch((e)=>{                expect(e.name).to.equal("Error");            });    });    it('Buscar cep via XML', function() {        cep.byXml('28035053')            .catch((e)=>{                expect(e.name).to.equal("Error");            });    });});