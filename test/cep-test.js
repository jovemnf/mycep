let cep = require('../index');let assert = require('assert');let expect = require('chai').expect;describe('myCep', function() {    it('Buscar cep via JSON', function() {        cep.json('28035053')            .catch((e) => expect(e.name).to.equal("Error"));    });    it('Buscar cep via XML', function() {        cep.xml('28035053')            .catch((e) => expect(e.name).to.equal("Error"));    });});